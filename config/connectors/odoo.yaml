# Odoo mock connector configuration.
#
# Connects to the C3 mock-source Odoo namespace.
# The mock-source reads from data/demo/*.csv and transforms
# rows into Odoo-style JSON payloads.

source_name: odoo_mock
source_type: mock
base_url: http://mock-source:8080
timeout: 10
